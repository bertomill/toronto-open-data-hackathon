export enum QueryType {
  SUMMARY = 'summary',
  TREND = 'trend',
  COMPARISON = 'comparison',
  RANKING = 'ranking',
  SPECIFIC = 'specific'
}

export enum MessageRole {
  USER = 'user',
  ASSISTANT = 'assistant',
  SYSTEM = 'system'
}

export enum DataViewMode {
  TABLE = 'table',
  JSON = 'json',
  CHART = 'chart'
} 